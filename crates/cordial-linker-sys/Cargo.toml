[package]
name = "cordial-linker-sys"
description = "Rust bindings to the AOSP bionic linker, built for the host"
version.workspace = true
edition.workspace = true
license.workspace = true
links = "cordial_linker_shim"

[lints]
workspace = true

[build-dependencies]
cmake = "0.1"
